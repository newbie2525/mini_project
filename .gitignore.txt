# 1. Deployment Artifacts and Generated Files
model_pickle # The pre-calculated similarity matrix model file generated by app.py
.DS_Store    # macOS specific files (was present in your uploaded files)
app.ipynb    # Jupyter Notebook file (often large and not needed for deployment)
*.pyc        # Python compiled bytecode
__pycache__/ # Python cache directories
/datasets    # Ignore the entire datasets folder to prevent uploading the CSV directly

# 2. Virtual Environments
venv/
.venv/

# 3. Vercel-specific Ignore (though not strictly necessary for gitignore, good to have)
.vercel/

# 4. Git Large File Storage (LFS) Pointer Files (if LFS is initialized, ignore the LFS cache)
.git/lfs/
